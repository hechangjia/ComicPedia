# ComicPedia 领域术语

ComicPedia 将主题编排成分镜、生成画面、复审作品并导出。模型配置与作品生成共用以下术语。

## 模型配置

**服务连接（Connection）**：可访问的一项模型服务及其连接方式，不等同于该服务里的某个模型。
_Avoid_：把地址、模型、角色统称为“模型”。

**模型（Model）**：服务连接下的一个明确模型标识；同一个模型可能具备多种能力。

**模型角色（Role binding）**：创作流程中的职责分配，包括分镜脚本、画面生成与视觉复审。分配角色不代表该能力已经验证。

**能力验证（Capability verification）**：对指定模型、指定职责实际输入与输出的检查；文字连通不能证明看图或出图能力。
_Avoid_：用“连接成功”代替所有能力通过。

**配置草稿（Configuration draft）**：用户正在编辑、尚未由服务器确认接受的配置；可以与当前生效配置不同。
_Avoid_：把本地草稿称为“已保存到服务器”。

**生效配置（Accepted configuration）**：服务器已确认保存的模型连接与角色选择，是后台创作任务读取配置的依据。

**配置冲突（Configuration conflict）**：草稿依据的生效配置已被另一操作更新，不能无提示覆盖。
